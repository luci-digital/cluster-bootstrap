[Service]
# Fix for linger startup - ensure runtime dir exists before user instance starts
ExecStartPre=/bin/bash -c 'mkdir -p /run/user/%i && chown %i:%i /run/user/%i && chmod 700 /run/user/%i'
Environment="XDG_RUNTIME_DIR=/run/user/%i"
